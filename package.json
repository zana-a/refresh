{
  "name": "workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "removeDest": "rm -Rf dest && rm -Rf docs/assets/css",
    "dev:build": "node-sass src -o docs/assets/css --indent-width=2 --output-style=expanded",
    "dev:watchSrc": "node-sass src -o docs/assets/css --indent-width=2 --output-style=expanded --watch",
    "dev": "npm run removeDest && npm run dev:build && npm run dev:watchSrc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.12.0"
  }
}
